#!/usr/bin/env sh
# Abbreviated gradle wrapper. Full wrapper auto-generated by Android Studio on first open,
# or by running `gradle wrapper` if you have Gradle installed system-wide.
echo "This is a stub gradlew. Open the android/ folder in Android Studio to auto-generate the full wrapper."
echo "Or run: gradle wrapper (requires Gradle 8.x installed)"
exit 1
